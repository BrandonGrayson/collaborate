import React from "react";
import { Grid } from "@mui/material";

export default function Navbar() {
  return (
    <Grid container sx={{ height: "100px", backgroundColor: "red" }}>
      <Grid item xs={12}></Grid>
    </Grid>
  );
}
